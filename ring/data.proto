syntax = "proto3";

package ring;

option go_package = "github.com/fgouteroux/acme_manager/ring";

// Data represents the certificate data stored in the memberlist KV store
message Data {
  string content = 1;
  int64 updated_at = 2;  // Unix timestamp in milliseconds (like Mimir)
}